#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--)
    {
        int n,k,l;
        cin>>n>>k>>l;
        int found=-1;
        int max=0;
        if(n<k){
        for(int i=0;i<n;i++){
           int m,v;
           cin>>m>>v;
           if(v==l){
           
           }
        }
        }
    }

    return 0;
}